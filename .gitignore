.quarto/
_site/
**/*.quarto_ipynb
/schema/generated/
# Local environment / secrets — never commit real keys (.env.example is tracked)
.env
.env.*
!.env.example

**/.DS_Store

# Python bytecode caches
**/__pycache__/
*.pyc

# Scratch LLM batch export dirs (contain copied paper full-text XML — do not commit)
/batch-smoke/